apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "micro-arch.fullname" . }}-test-connection"
  labels:
    {{- include "micro-arch.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "micro-arch.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
